1. Git Cherry-pick이란?
git cherry-pick은 체리 상자에서 가장 잘 익은 체리만 쏙 골라내듯, 다른 브랜치에 있는 특정 커밋 하나(또는 여러 개)만 가져와 현재 브랜치에 적용하는 기능입니다.

보통 전체 브랜치를 머지(git merge)하기엔 부담스럽고, 특정 버그 수정 커밋이나 기능 하나만 빠르게 메인 브랜치로 가져와야 할 때 매우 유용합니다.

2. 기본 사용법
① 가져올 커밋 해시(Commit Hash) 확인
먼저 가져오고 싶은 커밋이 있는 브랜치로 이동하거나, 로그를 확인하여 7자리 커밋 해시값을 확인합니다.

Bash
git log --oneline
출력 예시:

Plaintext
a1b2c3d (feature/login) Fix: 로그인 유효성 검사 로직 수정
e4f5g6h (feature/login) Add: 로그인 UI 레이아웃 구현
② 현재 작업 중인 브랜치로 이동 후 Cherry-pick 실행
가져온 커밋을 적용할 대상 브랜치(예: main 또는 develop)로 이동한 뒤 명령어를 입력합니다.

Bash
# 대상 브랜치로 이동
git checkout main

# 특정 커밋 가져오기
git cherry-pick a1b2c3d
3. 주요 활용 상황 & 꿀팁
여러 커밋 한 번에 가져오기

Bash
# 떨어진 커밋 여러 개
git cherry-pick a1b2c3d e4f5g6h

# 연속된 커밋 범위 (a1b2c3d 이후부터 e4f5g6h까지)
git cherry-pick a1b2c3d..e4f5g6h
충돌(Conflict) 발생 시 해결 방법

충돌이 난 파일을 수정합니다.

수정된 파일을 스테이징합니다: git add <파일명>

체리픽 작업을 이어갑니다: git cherry-pick --continue
(만약 작업을 취소하고 싶다면: git cherry-pick --abort)

4. 오늘 깨달은 점
브랜치 전체를 머지하지 않고도 필요한 작업 단위만 빠르게 반영할 수 있어 핫픽스(Hotfix) 대응 시 매우 효율적입니다.

다만 동일한 내용의 커밋이 다른 해시값으로 중복 생성될 수 있으므로, 남발하기보다는 필요한 상황에 맞춰 주의해서 사용해야겠습니다.
