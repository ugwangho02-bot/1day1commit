Phase 1. 기본기 다지기 & 핵심 개념 실습 (1~2주차)
스프링의 핵심 메커니즘을 작은 실행 가능한 코드로 구현하고 커밋합니다.

IoC/DI 컨테이너: @Component, @Bean, 생성자 주입과 필드 주입 비교 실습

AOP(관점 지향 프로그래밍): @Aspect를 활용한 메서드 실행 시간 측정 및 로깅 어노테이션 제작

Spring MVC 기본: @RestController, @PathVariable, @RequestBody 요청/응답 데이터 바인딩

Validation: @Valid, @NotNull, @Size 등을 활용한 DTO 요청 유효성 검증

예외 처리: @RestControllerAdvice와 @ExceptionHandler를 이용한 공통 에러 응답 객체(ErrorResponse) 구성

Phase 2. 데이터 접근 기술 (JPA & MySQL) (3~4주차)
데이터베이스 연동과 ORM 사용법을 다룹니다.

Spring Data JPA 기초: Entity 매핑, JpaRepository CRUD 구현

연관관계 매핑: @OneToMany, @ManyToOne 단방향/양방향 매핑 및 연관관계 편의 메서드 작성

Querydsl 연동: 동적 쿼리 작성, DTO 직접 조회(Projection), 페이징(Pageable) 구현

영속성 컨텍스트 학습: 1차 캐시, 지연 로딩(FetchType.LAZY)과 N+1 문제 해결(fetch join)

트랜잭션 제어: @Transactional 옵션(readOnly, propagation) 테스트 코드 작성

Phase 3. 실전 REST API 토이 프로젝트 (5~7주차)
하나의 주제(예: 도서 대여 시스템, 간단한 블로그, 일정 관리 앱)를 정해 완전한 서비스 API를 구축합니다.

인증/인가: Spring Security + JWT 기반 로그인/회원가입, Refresh Token 재발급 구현

파일 업로드: Amazon S3 또는 local storage 파일 업로드 API 개발

동시성 제어: Redis 또는 DB Lock(Optimistic/Pessimistic)을 활용한 재고/선착순 로직 구현

외부 API 연동: RestTemplate 또는 WebClient를 활용한 외부 Open API 호출

Phase 4. 테스트 & 테스트 주도 개발(TDD) (8~9주차)
안정적인 코드를 작성하기 위한 테스트 격리 및 Mocking 연습입니다.

단위 테스트(Unit Test): JUnit 5 + Mockito를 활용한 Service 레이어 독립 테스트

슬라이스 테스트: @WebMvcTest를 활용한 Controller 요청/응답 검증

통합 테스트: @SpringBootTest 및 Testcontainers를 활용한 실제 DB 연동 테스트

Phase 5. 배포, CI/CD 및 최적화 (10주차~)
개발한 애플리케이션을 운영 환경으로 올리고 성능을 개선합니다.

Dockerize: Spring Boot 애플리케이션 Dockerfile 작성 및 컨테이너 빌드

CI/CD 파이프라인: GitHub Actions를 활용한 자동 빌드/테스트/배포 구축

성능 최적화: Spring Cache + Redis 캐싱 적용, DB 인덱스 튜닝 전후 쿼리 성능 비교

모니터링: Spring Boot Actuator + Prometheus + Grafana 메트릭 수집 설정
