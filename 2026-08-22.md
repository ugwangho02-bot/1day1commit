구분,일일 리팩토링 작업 내용,커밋 메시지 예시 (refactor: / style:)
이름 개선,의미가 모호한 변수/함수/클래스명을 명확하게 변경 (예: d → elapsedDays),refactor: rename variable d to elapsedDays for clarity
매직 넘버 제거,코드에 직접 들어간 숫자/문자열을 의미 있는 상수로 추출 (예: 86400 → SECONDS_PER_DAY),refactor: replace magic numbers with named constants
조기 리턴 (Early Return),복잡하게 중첩된 if-else 문을 조기 리턴 구조로 바꿔 가독성 향상,refactor: simplify nested if conditions using early return
함수 분리,하나의 함수가 여러 일을 하고 있을 때 단일 책임(SRP)에 맞게 작은 함수로 쪼개기,refactor: extract user validation logic into separate method
데드 코드 삭제,"더 이상 사용하지 않는 주석 처리된 코드, 미사용 변수/import 제거",refactor: remove unused imports and dead code
타입 & 포맷팅,"타입 명시(TypeScript/Java 등) 추가, 코드 들여쓰기 및 줄바꿈 정돈",style: reformat code layout and add explicit return types
