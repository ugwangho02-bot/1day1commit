옵션 1: Docker (컨테이너 기초 개념)
제목: [TIL] Docker 기초: 컨테이너와 이미지의 차이점 정리

1. Docker Image vs Container
Docker Image: 애플리케이션을 실행하기 위해 필요한 모든 파일, 환경, 라이브러리를 하나로 묶은 읽기 전용(Read-Only) 템플릿.

Docker Container: 이미지를 바탕으로 독립된 공간에서 실제 실행되는 인스턴스. (클래스가 Image라면, 객체/인스턴스가 Container)

2. 핵심 명령어 3가지
Bash
# 이미지 다운로드 및 컨테이너 실행
docker run -d -p 8080:80 nginx

# 실행 중인 컨테이너 목록 확인
docker ps

# 컨테이너 중지
docker stop <CONTAINER_ID>
3. 오늘 느낀 점
가상머신(VM)과 달리 Host OS의 커널을 공유하여 가볍고 빠르게 동작한다는 점이 핵심이다. 개발 환경과 운영 환경의 격차를 줄이기 위해 필수적으로 익혀야겠다.
