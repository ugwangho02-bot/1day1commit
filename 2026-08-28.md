📅 [2026-08-28] Spring Boot REST API 구축 및 DTO 설계
💡 Today's Focus
Spring Boot 3 기반의 RESTful API Controller 계층 설계

Entity 대신 DTO(Data Transfer Object)를 사용하는 이유와 객체 간 변환 기법 적용

📝 What I Learned
Entity와 DTO의 분리

DB 테이블과 직접 매핑되는 Entity를 API 응답으로 직접 노출하면 데이터 오염 위험이 크다는 것을 확인.

클라이언트 요청/응답 스펙에 맞춘 RequestDTO / ResponseDTO를 별도로 정의하여 의존성을 분리함.

Spring Boot REST Controller 구현

@RestController와 @RequestMapping을 활용한 계층 구조화.

HTTP 메서드 매핑 (@GetMapping, @PostMapping) 및 상태 코드(ResponseEntity) 명시적 처리.

🛠️ Trouble Shooting & Insight
문제: Entity를 DTO로 변환할 때 반복적인 빌더/생성자 코드가 길어짐.

해결: 정적 팩토리 메서드(from / of) 패턴을 DTO 내부에 구현하여 변환 로직을 캡슐화함.
