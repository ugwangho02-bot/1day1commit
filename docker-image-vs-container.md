# 1. Docker 정리 마크다운 파일 작성
cat << 'EOF' > docker-image-vs-container.md
# Docker 이미지와 컨테이너의 차이점 정리

Docker를 처음 접할 때 가장 헷갈리는 개념 중 하나가 **이미지(Image)**와 **컨테이너(Container)**의 관계입니다. 

가장 쉬운 비유로 **이미지는 '클래스(Class)' 또는 '빵 틀'**이고, **컨테이너는 '인스턴스(Instance)' 또는 '빵'**입니다.

---

## 1. Docker 이미지 (Image)

* **정의**: 컨테이너를 생성하기 위한 **실행 가능 프로그램, 환경, 라이브러리, 소스 코드가 포함된 정적 파일(템플릿)**입니다.
* **특징**:
  * **불변성(Immutable)**: 한 번 생성된 이미지는 수정할 수 없으며, 변경이 필요하면 새 이미지를 빌드해야 합니다.
  * **레이어(Layer) 구조**: 파일 시스템의 변경 사항을 차곡차곡 쌓은 레이어 구조로 이루어져 있어, 중복되는 레이어는 재사용함으로써 저장 공간을 효율적으로 사용합니다.
  * **상태**: 파일 형태로 저장되어 있으며, 실행되지 않는 **정적인 상태**입니다.

---

## 2. Docker 컨테이너 (Container)

* **정의**: Docker 이미지를 바탕으로 **격리된 공간에서 실제로 실행되는 프로세스(동적 상태)**입니다.
* **특징**:
  * **가변성(Mutable)**: 컨테이너 내부에서 파일 생성, 수정, 삭제가 가능합니다. (단, 컨테이너가 삭제되면 기본적으로 내부 데이터도 함께 삭제됩니다.)
  * **독립성/격리성**: 호스트 OS 및 다른 컨테이너와 격리된 환경(CPU, 메모리, 네트워크 등)에서 작동합니다.
  * **1:N 관계**: 하나의 동일한 이미지로부터 여러 개의 독립된 컨테이너를 생성하여 동시에 실행할 수 있습니다.

---

## 3. 한눈에 비교하기

| 구분 | Docker 이미지 (Image) | Docker 컨테이너 (Container) |
| :--- | :--- | :--- |
| **비유** | 클래스(Class), 빵 틀, 설계도 | 인스턴스(Instance), 빵, 실제 건물 |
| **상태** | 정적 (Static / Read-Only) | 동적 (Dynamic / Read-Write) |
| **역할** | 컨테이너 생성용 템플릿 | 실제 애플리케이션 실행 환경 |
| **수정 여부** | 불가능 (수정 시 새 레이어 생성 필요) | 가능 (컨테이너 레이어에 읽기/쓰기) |
| **주요 명령** | `docker build`, `docker pull`, `docker images` | `docker run`, `docker ps`, `docker stop` |

---

## 4. 관계 및 동작 흐름

```text
[ Dockerfile ] ---> (docker build) ---> [ Docker Image ] ---> (docker run) ---> [ Docker Container ]
 (설계 명세서)                              (정적 템플릿)                           (실행 중인 프로세스)  
