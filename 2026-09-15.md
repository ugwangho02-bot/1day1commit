[오늘의 학습/Troubleshooting] Docker Container 실행 시 포트 포워딩 오해와 해결

1. 개요
오늘 작업 중 로컬 환경에서 Docker 컨테이너를 실행했으나 외부(브라우저)에서 서비스에 접근할 수 없는 문제가 발생했다. 원인을 분석하고 올바른 포트 포워딩(Port Forwarding) 명령어를 정리한다.

2. 문제 상황
docker run -d my-app 명령어로 컨테이너를 정상 실행함.

localhost:8080으로 접속을 시도했으나 ERR_CONNECTION_REFUSED 에러 발생.

3. 원인 분석
Docker 컨테이너는 격리된 네트워크 환경에서 동작한다.

컨테이너 내부에서 특정 포트(예: 8080)를 열어두었더라도, 호스트(내 PC)의 포트와 연결(-p 옵션)해주지 않으면 외부 네트워크에서 접근할 수 없다.

4. 해결 방법
호스트 포트와 컨테이너 포트를 매핑하여 컨테이너를 다시 실행했다.

Bash
# -p [호스트 포트]:[컨테이너 포트]
docker run -d -p 8080:8080 my-app
호스트 포트(앞): 내 PC 브라우저에서 접근할 포트

컨테이너 포트(뒤): 컨테이너 내부 애플리케이션이 수신 대기 중인 포트

5. 오늘의 배운 점 (TL;DR)
docker run 시 -p 옵션을 빠뜨리면 외부 접근이 불가능하다.

포트 매핑 구조 [Host]:[Container]의 순서를 헷갈리지 말자.
