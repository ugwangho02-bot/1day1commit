📄 [Daily Log] API 개념 이해 및 Requests 라이브러리 활용 실습
📅 Date: 2026. 09. 02

🏷️ Topic: REST API, Python requests, Status Code

💡 1. Today's Summary
핵심 학습 항목: API(Application Programming Interface)의 기본 동작 원리 및 RESTful API 규격 이해

구현 / 해결 내용: Python requests 모듈을 사용하여 외부 API 통신 및 JSON 데이터 파싱 실습 완료

🔍 2. Deep Dive
API 및 REST API 개념

API: 클라이언트와 서버 간 데이터를 주고받기 위한 규약

HTTP 메서드: GET(조회), POST(생성), PUT(수정), DELETE(삭제)

Python을 이용한 GET 요청 실습

Python
import requests

# API EndPoint 호출
url = "https://jsonplaceholder.typicode.com/posts/1"
response = requests.get(url)

# 상태 코드 및 데이터 확인
if response.status_code == 200:
    data = response.json()
    print(f"Title: {data['title']}")
else:
    print(f"Error: {response.status_code}")
🛠️ 3. Troubleshooting
문제: API 호출 시 401 Unauthorized 에러 발생

원인: Header에 API Key 인증 정보를 누락하여 권한 거부됨

해결: headers = {"Authorization": f"Bearer {API_KEY}"} 구문을 작성하여 요청 헤더에 인증키 추가 전달

🎯 4. Tomorrow's Goal
[ ] POST 메서드를 통한 데이터 생성 테스트

[ ] API Key 보안 관리를 위한 .env 환경 변수 설정 실습
