# [TIL] Python List vs Set - 속도 비교 및 사용 시점 정리

1일 1커밋 Challenge - Day 1

오늘 프로젝트를 진행하면서 데이터 중복 제거 및 포함 여부(`in` 연산자)를 확인할 때 `list`와 `set` 중 무엇을 사용해야 할지 성능 측면에서 비교정리했습니다.

---

## 1. 주요 차이점 핵심 요약

| 구분 | List (리스트) | Set (집합) |
| :--- | :--- | :--- |
| **순서 보장** | O (인덱스 접근 가능) | X (순서 없음) |
| **중복 허용** | O | X (자동 중복 제거) |
| **탐색 시간 복잡도** | **O(N)** (선형 탐색) | **O(1)** (해시 테이블) |

---

## 2. 탐색 속도 비교 (`in` 키워드)

* **List 탐색:** 첫 번째 요소부터 끝까지 순차적으로 확인하므로 데이터 수($N$)에 비례하여 시간이 늘어납니다.
* **Set 탐색:** 해시 함수를 이용해 해당 값이 위치한 주소를 즉시 조회하므로, 데이터 양과 상관없이 거의 일정 **O(1)**한 시간이 걸립니다.

```python
import time

# 1,000만 개의 데이터 생성
data_list = list(range(10000000))
data_set = set(data_list)

# List 탐색 시간 측정
start = time.time()
9999999 in data_list
print(f"List 탐색 시간: {time.time() - start:.6f}초")

# Set 탐색 시간 측정
start = time.time()
9999999 in data_set
print(f"Set 탐색 시간: {time.time() - start:.6f}초")
